name: Test
on: push
jobs:
  tarea1:
    name: primera
    runs-on: ubuntu-latest
    steps:
      - name: Create Github Issue
        uses: DanielBarnes/create-issue-action@v1
        with:
          title: "Push asociado"
